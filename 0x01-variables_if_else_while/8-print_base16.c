#include <stdio.h>
/**
 * main - prints all numbers in base 16
 *
 * Return: 0 always (success)
 */
int main(void)
{
	int a = 48;
	int c;

	for (a = 48; a < 58; a++)
		putchar(a);
		while (c < 'g')
		{
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
