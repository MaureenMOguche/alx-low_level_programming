#include <stdio.h>
/**
 * main - prints 0 to 9
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int a;
	char c[10];

	for (a = 0; a < 10; a++)
	{
		c[10] = a;
	}
	printf("%s\n", c);
	return (0);
}
